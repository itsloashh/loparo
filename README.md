# Loparo v3 — 3D Landing + Puzzle Glassmorphism

Next.js 14 + Three.js + React Three Fiber + Tailwind CSS + TypeScript

## Setup
```bash
npm install
npm run dev
```

## Deploy
Push to GitHub → Vercel auto-deploys.
Set `NEXT_PUBLIC_WEBHOOK_URL` in Vercel env variables.
